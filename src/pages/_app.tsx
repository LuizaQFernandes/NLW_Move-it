//componentes fixos o ChallengesContext.Provider da acesso aos dados do contexto
import '../styles/global.css'

function MyApp({ Component, pageProps }) {
 
  return(
      <Component {...pageProps} />
  ) 
}

export default MyApp
